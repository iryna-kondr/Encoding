// Insight
export default class Insight {
  text: string // The peak of new hires was in September
  attribute: string // New Hires (needed?)
  
  constructor(text: string, attribute: string) {
    this.text = text
    this.attribute = attribute
  }
}